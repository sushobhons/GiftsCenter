trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: ExtractFiles@1
  inputs:
    archiveFilePatterns: '**/*.zip'
    destinationFolder: '$(Pipeline.Workspace)/extracted-files'
    cleanDestinationFolder: true
    overwriteExistingFiles: false